<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connection Offline</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background-color: #f7f7f7;
            color: #5f6368;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            padding: 20px;
            box-sizing: border-box;
        }

        .error-container {
            max-width: 400px;
            text-align: left;
        }

        .error-icon {
            font-size: 50px;
            margin-bottom: 20px;
            color: #757575;
            user-select: none;
        }

        h1 {
            font-size: 22px;
            font-weight: 500;
            color: #202124;
            margin: 0 0 10px 0;
            line-height: 1.3;
        }

        p {
            font-size: 14px;
            line-height: 1.6;
            margin: 0 0 20px 0;
        }

        .error-code {
            font-family: monospace;
            font-size: 12px;
            color: #757575;
            text-transform: uppercase;
        }

        .admin-trigger {
            position: fixed;
            bottom: 20px;
            right: 20px;
            padding: 8px 16px;
            background: #e8eaed;
            color: #5f6368;
            border: 1px solid #dadce0;
            border-radius: 4px;
            font-size: 13px;
            cursor: pointer;
        }
        .admin-trigger:hover {
            background: #f1f3f4;
        }

        .pin-overlay {
            display: none;
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(32, 33, 36, 0.95);
            justify-content: center;
            align-items: center;
            flex-direction: column;
            z-index: 9999;
        }

        .pin-display {
            font-size: 36px;
            margin-bottom: 25px;
            letter-spacing: 8px;
            height: 45px;
            color: #fff;
        }

        .pin-grid {
            display: grid;
            grid-template-columns: repeat(3, 80px);
            grid-gap: 15px;
        }

        .pin-btn {
            width: 80px;
            height: 80px;
            font-size: 24px;
            background: #3c4043;
            color: white;
            border: none;
            border-radius: 50%;
            cursor: pointer;
        }

        .pin-btn:active {
            background: #5f6368;
        }

        .action-btn {
            background: #ea4335;
        }
        .action-btn:active {
            background: #c5221f;
        }
    </style>
</head>
<body>

    <div class="error-container">
        <div class="error-icon">â ï¸</div>
        <h1>This site canât be reached</h1>
        <p>The internet connection dropped or the server is temporarily down. Check your network cables or restart the device configuration.</p>
        <div class="error-code">ERR_INTERNET_DISCONNECTED</div>
    </div>

    <button class="admin-trigger" onclick="togglePinPad(true)">Admin Exit</button>

    <div class="pin-overlay" id="pinOverlay">
        <div class="pin-display" id="pinDisplay">****</div>
        <div class="pin-grid">
            <button class="pin-btn" onclick="pressKey('1')">1</button>
            <button class="pin-btn" onclick="pressKey('2')">2</button>
            <button class="pin-btn" onclick="pressKey('3')">3</button>
            <button class="pin-btn" onclick="pressKey('4')">4</button>
            <button class="pin-btn" onclick="pressKey('5')">5</button>
            <button class="pin-btn" onclick="pressKey('6')">6</button>
            <button class="pin-btn" onclick="pressKey('7')">7</button>
            <button class="pin-btn" onclick="pressKey('8')">8</button>
            <button class="pin-btn" onclick="pressKey('9')">9</button>
            <button class="pin-btn action-btn" onclick="togglePinPad(false)">X</button>
            <button class="pin-btn" onclick="pressKey('0')">0</button>
            <button class="pin-btn action-btn" onclick="clearPin()">C</button>
        </div>
    </div>

    <script>
        const CORRECT_PIN = "1234"; 
        let currentInput = "";

        function togglePinPad(show) {
            document.getElementById('pinOverlay').style.display = show ? 'flex' : 'none';
            clearPin();
        }

        function pressKey(num) {
            if (currentInput.length < 4) {
                currentInput += num;
                updateDisplay();
                
                if (currentInput === CORRECT_PIN) {
                    window.close();
                } else if (currentInput.length === 4) {
                    setTimeout(clearPin, 500);
                }
            }
        }

        function clearPin() {
            currentInput = "";
            updateDisplay();
        }

        function updateDisplay() {
            let dots = "";
            for(let i=0; i < currentInput.length; i++) {
                dots += "â";
            }
            document.getElementById('pinDisplay').innerText = dots || "****";
        }
    </script>
</body>
</html>
